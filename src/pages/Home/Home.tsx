import { Container } from "react-bootstrap";

const Home = () => {
  return (
    <Container style={{ height: "100vh" }} className="d-flex flex-column align-items-center">
    </Container>
  );
};

export default Home;
